---
description: List the .slang workflows available to run
allowed-tools: mcp__slang-workflows__list_workflows
---

Call `list_workflows` and present every discovered workflow as a compact table — name, title, params, and agent count. Then note that the user can start one with `/slang-workflows:slang-run <name>`.
