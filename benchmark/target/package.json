{
  "name": "slang-bench-target",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Self-contained TypeScript + vitest scaffold — the codebase the benchmark's agents implement features into. Replaces the internal repo so the benchmark runs anywhere.",
  "scripts": {
    "test": "vitest run",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
