{
  "name": "slang-workflows-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic, .slang-driven multi-agent workflow executor as a Claude Code MCP server.",
  "license": "MIT",
  "main": "src/main.ts",
  "_dispatchDep": "@anthropic-ai/claude-agent-sdk is the dispatch-layer dependency, kept optional because the internal registry proxy does not currently mirror it. The executor depends on it only through the Dispatcher interface; promote it to dependencies once reachable.",
  "scripts": {
    "dev": "tsx src/main.ts",
    "start": "tsx src/main.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test --import tsx test/*.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "zod": "3.25.76"
  },
  "optionalDependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.187"
  },
  "devDependencies": {
    "@esbuild/linux-x64": "0.28.1",
    "@types/node": "24.2.1",
    "tsx": "4.22.4",
    "typescript": "5.8.3"
  }
}
